generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "sqlite"
}

model Product {
  @@map("products")

  id          Int      @id @default(autoincrement())
  name        String
  stock       Int
}