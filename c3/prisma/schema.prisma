generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "sqlite"
}

model Score {
  @@map("scores")

  id          Int      @id @default(autoincrement())
  player_name String
  score       Int
}